# ObscuraCRT
About Java-based desktop application for image steganography using the Least Significant Bit (LSB) technique. The project focuses on embedding and extracting secret text messages inside image files without changing visible image quality. Built with Java Swing for an intuitive GUI and uses BufferedImage for efficient pixel-level image processing.
